name: Deploy React to Hugo

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the frontend project
      - name: Checkout frontend repo
        uses: actions/checkout@v3

      # Step 2: Setup Node
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # Step 3: Install dependencies & build React
      - name: Install dependencies and build
        working-directory: frontend
        run: |
          npm install
          npm run build

      # Step 4: Checkout Hugo gh-pages branch
      - name: Checkout Hugo repo
        uses: actions/checkout@v3
        with:
          repository: IncreaseUnusual/danison-blog
          path: hugo
          ref: gh-pages
          token: ${{ secrets.HUGO_REPO_TOKEN }}

      - name: Copy build to Hugo /qup-app
        run: |
          rm -rf hugo/qup-app
          mkdir -p hugo/qup-app
          cp -r frontend/dist/* hugo/qup-app/

      - name: Commit and push to Hugo
        run: |
          cd hugo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Auto-deploy React app to /qup-app [CI]" || echo "Nothing to commit"
          git push origin gh-pages
